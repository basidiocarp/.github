name: Docs Validation

on:
  pull_request:
    paths:
      - "docs/**"
      - "profile/**"
      - "scripts/validate-docs.py"
      - ".github/workflows/docs-validation.yml"
  push:
    branches:
      - main
    paths:
      - "docs/**"
      - "profile/**"
      - "scripts/validate-docs.py"
      - ".github/workflows/docs-validation.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: docs-validation-${{ github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2

      - name: Validate docs
        run: python3 scripts/validate-docs.py
